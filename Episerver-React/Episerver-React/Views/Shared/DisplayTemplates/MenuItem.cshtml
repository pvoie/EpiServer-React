@model List<string>

@if (Model.Count > 0)
{
    var searchUrl = ViewData["SearchUrl"];
    foreach (var item in Model)
    {
        <li>
            <a href="@string.Format("{0}?filters={1}",searchUrl,item)" title="@item">@item</a>
        </li>
    }
}

